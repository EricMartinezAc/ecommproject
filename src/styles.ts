import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  card: {
    marginBottom: 16,
  },
  productImage: {
    width: "100%",
    height: 200,
    marginTop: 8,
  },
  input: {
    marginBottom: 16,
  },
  fileInput: {
    marginBottom: 16,
  },
});

export default styles;
